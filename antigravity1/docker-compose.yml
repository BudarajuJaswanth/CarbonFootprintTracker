services:
  api:
    build:
      context: .
      dockerfile: Dockerfile.server
    ports:
      - "3333:3333"
    environment:
      - NODE_ENV=production

  frontend:
    build:
      context: .
      dockerfile: Dockerfile.frontend
    ports:
      - "5173:80"
    depends_on:
      - api
